export interface Category {
  id: string
  name: string
  name_ar: string
  slug: string
  created_at: string
}

export interface Area  {
    id: string
    name: string
    name_ar: string
    slug: string
    created_at: string
}
